
DATABASE_URL=postgresql://postgres:password@localhost:5432/ordersdb
REDIS_URL=redis://localhost:6379
LOG_LEVEL=INFO

# Optional — change the PostgreSQL LISTEN channel name if you're
# running multiple instances of this service on the same DB.
PG_NOTIFY_CHANNEL=order_changes
REDIS_CHANNEL=order_changes
